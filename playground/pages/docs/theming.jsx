export default function ThemingPage() {
  return (
    <>
      <h2>Theming</h2>

      <p>
        <em>
          Stub — config-file theming (<code>vanillin.config.json</code> →
          generated CSS) is planned and will replace this page.
        </em>
      </p>

      <p>
        Every design token lives in <code>styles/globals.css</code>; token
        names match shadcn/ui. Edit that one file to retheme everything, and
        add a <code>.dark</code> class on <code>&lt;html&gt;</code> for dark
        mode.
      </p>
    </>
  )
}
